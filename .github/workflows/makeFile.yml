name: C/C++ CI

on:
  push:
    branches: [ BranchForPull ]
  pull_request:
    branches: [ BranchForPull ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make -C Implementation/ all
